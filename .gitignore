# Project register: live data holding client names. Deliberately NOT committed,
# so pushing this repo to a remote never publishes client information.
projects.csv

# Generated docs are committed on purpose - they are the readable standard.

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
*.swp
*~
__pycache__/
*.pyc
.venv/
.idea/
.vscode/

# Built templates are outputs; master/ is the source of truth.
_tool/templates/.medtpl-check/
_tool/templates/.medtpl-rt/
_tool/templates/.medtpl-rt.docx
# 02-Sablonok IS committed: the built templates are the point of the repo
# for anyone who just wants to use them without running Python.
